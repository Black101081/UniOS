#include <stdio.h>
#include <stdlib.h>

// Khởi động kernel cơ bản
void kernel_main() {
    printf("UniOS Kernel Initialized!\n");
    while (1) {
        // Kernel loop (chờ xử lý các tác vụ)
    }
}